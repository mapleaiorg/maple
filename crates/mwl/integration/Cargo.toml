[package]
name = "maple-mwl-integration"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Legacy compatibility wrapper (prefer maple-worldline-integration-suite or worldline-integration)"

# This is a test-only crate — no library output
[lib]
name = "mwl_integration"
path = "src/lib.rs"

[dependencies]
worldline-integration = { workspace = true }
